<%= form_for(@idea ,:html => {:class => 'form-horizontal'}) do |f| %>
  <% if @idea.errors.any? %>
	<div class="alert">
	  <button class="close" data-dismiss="alert">×</button>
		<h2><%= pluralize(@idea.errors.count, "error") %> prohibited this idea from being saved:</h2>

	    <ul>
	    <% @idea.errors.full_messages.each do |msg| %>
	      <li><%= msg %></li>
	    <% end %>
	    </ul>
	</div>
  <% end %>
	<fieldset>
		<legend>Whatcha thinking about?</legend>
		<div class="control-group">
		  <%= f.label :name, :class => 'control-label' %>
			<div class="controls">
		  		<%= f.text_field :name, :placeholder => "Your idea here", :class => 'input-xlarge' %>
				<p class="help-block">Keep it short and sweet</p>
			</div>
		</div>

		<div class="control-group">
		  <%= f.label :description, :class => 'control-label' %>
			<div class="controls">	
		  		<%= f.text_area :description, :placeholder => "What's it all about? Go nuts.", :class => 'input-xlarge' %>
			</div>
		</div>
		<div class="control-group">
		  <%= f.label :author, :class => 'control-label' %>
			<div class="controls">
		  		<%= f.text_field :author, :placeholder => "What's your name?", :class => 'input-xlarge' %>
			</div>
		</div>
	</fieldset>
  <div class="form-actions">
    <%= f.submit :class => 'btn' %>
  </div>
<% end %>